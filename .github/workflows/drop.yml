name: DropWizard EndPoint Action workflow

on:
  workflow_dispatch:
    inputs:
      NODE_TYPE:
        required: false
        description: Node type

env:
  NODE_TYPE :  "${{ inputs.NODE_TYPE }}== '' ? 'dind-nocache' : ${{ inputs.NODE_TYPE }}"

jobs:
  dropwizard:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
      - name: Check Performance Test Environments
        run: |
          echo "${{ env.NODE_TYPE }}"
          
